apiVersion: k8s.otterize.com/v1alpha3
kind: ClientIntents
metadata:
  name: ack-dynamodb
spec:
  service:
    name: dynamodb-operator-dynamodb-chart
  calls:
  - name: "*"
    type: aws
    awsActions:
        - "dynamodb:*"
